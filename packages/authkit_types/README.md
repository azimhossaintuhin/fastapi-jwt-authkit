# fastapi-authkit-stubs

Type stubs package for `fastapi-authkit` (PEP 561).

Install:

```bash
pip install fastapi-authkit[stubs]
```

This package contains only `.pyi` and `py.typed` files for static type checkers.
