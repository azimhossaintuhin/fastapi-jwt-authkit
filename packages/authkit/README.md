# fastapi-authkit

Authentication toolkit for FastAPI with JWT, supporting sync and async
SQLAlchemy backends.

Install:

```bash
pip install fastapi-authkit
```

Optional stubs:

```bash
pip install fastapi-authkit[stubs]
```
